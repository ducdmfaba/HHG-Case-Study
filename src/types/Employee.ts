export interface InfoEmployee {
  id: string;
  createdAt: string;
  name: string;
  email: string;
  position: string;
}

export interface InfoAddEmployee {
  name: string;
  email: string;
  position: string;
}
